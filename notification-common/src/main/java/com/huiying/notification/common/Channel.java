package com.huiying.notification.common;

/** Delivery channel. Also used as the SNS message attribute that drives subscription filter policies. */
public enum Channel {
    EMAIL,
    SMS,
    PUSH
}
