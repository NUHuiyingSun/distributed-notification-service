## What does this change?

## Why?

## How was it tested?
- [ ] `mvn verify` passes
- [ ] `./scripts/e2e-test.sh` passes locally (if the change touches the delivery pipeline)
- [ ] Docs updated (`README.md` / `docs/DESIGN.md`) if behavior changed
